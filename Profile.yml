info:
  name: Profile
  type: http
  seq: 5

http:
  method: GET
  url: https://allalca.alca.life/api/Profile
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer "
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
